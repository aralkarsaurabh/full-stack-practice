// Q1
// Create a type alias for a string called Username.
// Create a variable of that type and print it.

// Q2
// Create a type alias for a number called Price.
// Create a variable of that type and print it.

// Q3
// Create a type alias for an object called Point with:
// x (number) and y (number).
// Create two Point variables and print them.

// Q4
// Create a type alias called Status using a union type:
// "active" | "inactive" | "pending".
// Create a variable of that type and print it.

// Q5
// Create a type alias called ID that can be either a string or a number.
// Create two variables — one with a string ID and one with a number ID.

// Q6
// Create a type alias called Coordinate with:
// latitude (number) and longitude (number).
// Create a function that takes a Coordinate and prints it.

// Q7
// Create a type alias for a function called MathOperation:
// It takes two numbers and returns a number.
// Use it to type an add function and a multiply function.

// Q8
// Create a type alias called Color with the union:
// "red" | "green" | "blue" | "yellow".
// Write a function that takes a Color and prints a message about it.

// Q9
// Create a type alias called StringOrArray
// that can be either a string or an array of strings.
// Write a function that takes StringOrArray and prints its length.

// Q10
// Create a type alias for an Address object with:
// street (string), city (string), country (string).
// Create a type alias for a Person that includes the Address type.
// Create a Person object and print it.

// Q11
// Create a type alias called Callback:
// A function that takes a string message and returns void.
// Write a function that accepts a Callback and calls it with "Done!".

// Q12
// Create a type alias called Result with:
// success (boolean) and data (string).
// Write a function that returns a Result object.

// Q13
// Create a type alias called Direction with the union:
// "north" | "south" | "east" | "west".
// Write a function that takes a Direction and prints a movement message.

// Q14
// Create a type alias called UserRole with the union:
// "admin" | "editor" | "viewer".
// Write a function that takes a UserRole and prints the permissions for that role.

// Q15
// Create a type alias called ApiResponse with:
// status (number), message (string), and optionally data (object).
// Write a function that simulates an API response and returns ApiResponse.
// Print the result.
