// Q1
// Create a class Person with:
// properties: name (string), age (number).
// A method greet() that prints "Hi, I am <name> and I am <age> years old."
// Create an instance and call greet().

// Q2
// Create a class Rectangle with:
// properties: width (number), height (number).
// Methods: area() and perimeter().
// Create an instance and print both.

// Q3
// Create a class BankAccount with:
// properties: accountHolder (string), balance (number).
// Methods: deposit(amount: number) and withdraw(amount: number).
// Print balance after each operation.

// Q4
// Create a class Student with:
// properties: name (string), marks (number[]).
// Method: getAverage() that returns the average of marks.
// Create an instance and print the average.

// Q5
// Create a class Car with:
// properties: brand (string), speed (number).
// Method: accelerate(amount: number) that increases speed.
// Method: brake(amount: number) that decreases speed.
// Print speed after each call.

// Q6
// Create a class Animal with:
// properties: name (string), sound (string).
// Method: speak() that prints "<name> says <sound>."
// Create a class Dog that extends Animal.
// Override speak() to print "<name> barks!"
// Create instances of both and call speak().

// Q7
// Create a class Counter with:
// A private property count starting at 0.
// Methods: increment(), decrement(), and getCount().
// Create an instance and call each method.

// Q8
// Create a class Circle with:
// property: radius (number).
// Methods: area() and circumference().
// (Use Math.PI)
// Create an instance and print both.

// Q9
// Create a class Employee with:
// properties: name (string), salary (number), department (string).
// Method: getDetails() that prints all details.
// Method: applyRaise(percent: number) that increases salary.
// Create an instance and apply a raise.

// Q10
// Create a class Stack with:
// A private array to hold items.
// Methods: push(item: string), pop(), peek(), and isEmpty().
// Create an instance and demonstrate all methods.

// Q11
// Create a class Vehicle with:
// property: brand (string), fuelLevel (number).
// Methods: refuel(amount: number) and drive(km: number).
// (Driving reduces fuelLevel by 1 per km.)
// Create a class Truck that extends Vehicle and adds: payload (number).
// Create an instance of Truck and print details.

// Q12
// Create a class TodoList with:
// A private array of todos (string[]).
// Methods: addTodo(task: string), removeTodo(task: string), and listTodos().
// Create an instance and demonstrate all methods.

// Q13
// Create a class Shape with a method area(): number that returns 0.
// Create subclasses Rectangle and Triangle that override area().
// Create instances of each and print their areas.

// Q14
// Create a class Temperature with:
// A private value stored in Celsius.
// Methods: toCelsius(), toFahrenheit(), toKelvin().
// Create an instance with a temperature and print all three formats.

// Q15
// Create a class Library with:
// A private array of books (string[]).
// Methods: addBook(title: string), removeBook(title: string), searchBook(title: string): boolean, and listBooks().
// Create an instance and demonstrate all methods.
