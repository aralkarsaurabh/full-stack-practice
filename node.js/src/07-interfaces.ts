// Q1
// Create an interface for a Person with:
// name (string), age (number), city (string).
// Create an object using this interface and print it.

// Q2
// Create an interface for a Car with:
// brand (string), model (string), year (number), isElectric (boolean).
// Create two car objects and print them.

// Q3
// Create an interface for a Product with:
// name (string), price (number), quantity (number).
// Add a method getTotalValue() that returns price × quantity.

// Q4
// Create an interface for a Student with:
// name (string), rollNumber (number), marks (number[]).
// Create an object and print the average marks using a method getAverage().

// Q5
// Create an interface BankAccount with:
// accountHolder (string), balance (number).
// Add methods deposit(amount: number) and withdraw(amount: number).

// Q6
// Create an interface Address with:
// street (string), city (string), pincode (string).
// Create an interface Employee that includes Address and also has:
// name (string), department (string).

// Q7
// Create an interface Shape with a method area(): number.
// Create two objects — one for a rectangle and one for a circle —
// that both implement this interface.

// Q8
// Create an interface for a Movie with:
// title (string), director (string), year (number), rating (number).
// Add an optional field language (string).
// Create two movie objects — one with and one without the optional field.

// Q9
// Create an interface Animal with:
// name (string), sound (string).
// Add a method speak() that prints "<name> says <sound>".
// Create objects for a Dog and a Cat.

// Q10
// Create an interface for a User with:
// username (string), email (string), isActive (boolean).
// Create a function that takes a User and prints their details.

// Q11
// Create an interface Order with:
// orderId (number), product (string), quantity (number), price (number).
// Add a method getTotal() that returns quantity × price.
// Create an order object and print the total.

// Q12
// Create an interface Vehicle with:
// brand (string), speed (number).
// Add a method describe() that prints:
// "<brand> can go at <speed> km/h".
// Create objects for a Bike and a Truck.

// Q13
// Create an interface Calculator with methods:
// add(a: number, b: number): number
// subtract(a: number, b: number): number
// multiply(a: number, b: number): number
// divide(a: number, b: number): number
// Create an object that implements this interface.

// Q14
// Create an interface LibraryBook with:
// title (string), author (string), isAvailable (boolean).
// Add a method checkout() that sets isAvailable to false and prints a message.
// Add a method returnBook() that sets isAvailable to true and prints a message.

// Q15
// Create an interface Profile with:
// name (string), bio (string), followers (number).
// Create an interface Admin that extends Profile and adds:
// role (string), permissions (string[]).
// Create an Admin object and print all properties.
